import React from "react";
import { SafeAreaView } from "react-native";
import { SafeAreaProvider } from "react-native-safe-area-context";
import { GPScreen } from "./src/modules/screen";

export default function App() {
  return <GPScreen />;
}
